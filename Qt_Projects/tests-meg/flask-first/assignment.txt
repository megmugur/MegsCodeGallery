Also I was thinking as an exercise, you could try running a flask server and having a qt app send data to it, have the flask server process it somehow and send something back to the app

Maybe use matplotlib along with flask?